import {MerlyLogo} from "@/components/merly-logo";import { LoginForm } from "./login-form";
export default async function Login({ searchParams }: { searchParams: Promise<{ setup?: string }> }){const sp=await searchParams;return <main className="mx-auto flex min-h-screen max-w-md flex-col justify-center px-4"><MerlyLogo variant="auth"/><div className="card mt-8"><h1 className="text-3xl font-bold text-merly-900">Đăng nhập CTV</h1><p className="mt-2 text-stone-600">Đăng nhập bằng email hoặc số điện thoại đã được Merly kích hoạt.</p><LoginForm setupSuccess={sp.setup === "success"}/></div></main>}
