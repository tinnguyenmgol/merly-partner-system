import { DashboardShell } from "@/components/layout/dashboard-shell";import { requireAdminSession } from "@/features/auth/admin-auth";import { getLeaderboard,getVietnamMonthRange,getVietnamWeekRange,type LeaderboardMetric } from "@/features/leaderboards";import { formatVnd } from "@/lib/money";export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<{range?:string;metric?:LeaderboardMetric}>}){await requireAdminSession();const sp=await searchParams;const metric=sp.metric==="orders"?"orders":"revenue";const range=sp.range==="month"?getVietnamMonthRange():getVietnamWeekRange();const board=await getLeaderboard(metric,range,"referral_ctv",50);return <DashboardShell admin><div className="space-y-6"><h1 className="text-3xl font-bold text-merly-900">Bảng xếp hạng</h1><form className="card flex flex-wrap gap-3"><select name="range" defaultValue={sp.range??"week"} className="rounded-xl border p-3"><option value="week">Tuần</option><option value="month">Tháng</option></select><select name="metric" defaultValue={metric} className="rounded-xl border p-3"><option value="revenue">Doanh thu</option><option value="orders">Số đơn</option></select><select name="partnerType" className="rounded-xl border p-3"><option value="referral_ctv">referral_ctv</option></select><button className="btn-primary">Lọc</button></form><div className="card overflow-x-auto"><p className="text-sm text-stone-500">Chỉ tính đơn hợp lệ theo chính sách CTV.</p><table className="mt-4 min-w-full text-left text-sm"><thead><tr><th>Hạng</th><th>CTV</th><th>Đơn hợp lệ</th><th>Doanh thu hợp lệ</th><th>Hoa hồng tạm tính</th></tr></thead><tbody>{board.rows.map(r=><tr className="border-t" key={r.partnerId}><td className="py-3">#{r.rank}</td><td>{r.displayName}</td><td>{r.orderCount}</td><td>{formatVnd(r.revenue)}</td><td>{formatVnd(r.commission)}</td></tr>)}</tbody></table></div></div></DashboardShell>}
