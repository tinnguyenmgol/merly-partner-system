import Link from "next/link";import { DashboardShell } from "@/components/layout/dashboard-shell";import { requireAdminSession } from "@/features/auth/admin-auth";import { db } from "@/lib/db";import { formatVnd } from "@/lib/money";export const dynamic="force-dynamic";const statuses=["invited","registered","approved","first_valid_order","reward_pending","rewarded","rejected"];export default async function Page({searchParams}:{searchParams?:Promise<{status?:string}>}){await requireAdminSession();const sp=await searchParams;const refs=await db.partnerReferral.findMany({where:sp?.status?{status:sp.status}:{},include:{referrerPartner:{select:{id:true,displayName:true}},referredPartner:{select:{id:true,displayName:true,status:true}}},orderBy:{createdAt:"desc"}});return <DashboardShell admin><h1 className="text-3xl font-bold text-merly-900">Giới thiệu partner</h1><form className="mt-4 flex gap-3"><select name="status" defaultValue={sp?.status??""} className="input w-auto"><option value="">Tất cả trạng thái</option>{statuses.map(s=><option key={s}>{s}</option>)}</select><button className="btn-secondary">Lọc</button></form><div className="card mt-6 overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="text-stone-500"><tr><th className="py-2">Người giới thiệu</th><th>Partner được giới thiệu</th><th>Trạng thái</th><th>Thưởng</th><th>Cập nhật thủ công</th></tr></thead><tbody>{refs.map(r=><tr className="border-t align-top" key={r.id}><td className="py-3"><Link className="font-semibold text-merly-700" href={`/admin/partners/${r.referrerPartnerId}`}>{r.referrerPartner.displayName}</Link></td><td>{r.referredPartner?<Link className="font-semibold text-merly-700" href={`/admin/partners/${r.referredPartner.id}`}>{r.referredPartner.displayName}</Link>:"Chưa gắn partner"}</td><td>{r.status}</td><td>{r.rewardAmount?formatVnd(r.rewardAmount):r.rewardDescription??"—"}</td><td><form method="post" action="/admin/partner-referrals/update" className="grid gap-2 min-w-56"><input type="hidden" name="id" value={r.id}/><select name="status" defaultValue={r.status} className="input">{statuses.map(s=><option key={s}>{s}</option>)}</select><input name="rewardAmount" defaultValue={r.rewardAmount??""} className="input" placeholder="Số tiền"/><input name="rewardDescription" defaultValue={r.rewardDescription??""} className="input" placeholder="Mô tả thưởng"/><input name="adminNote" defaultValue={r.adminNote??""} className="input" placeholder="Ghi chú admin"/><button className="btn-secondary">Lưu</button></form></td></tr>)}</tbody></table>{refs.length===0?<p className="p-4 text-stone-500">Chưa có referral phù hợp.</p>:null}</div></DashboardShell>}
