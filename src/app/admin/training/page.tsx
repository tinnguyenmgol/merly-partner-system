import { DashboardShell } from "@/components/layout/dashboard-shell";import { requireAdminSession } from "@/features/auth/admin-auth";import { db } from "@/lib/db";export const dynamic="force-dynamic";const cats=["Bắt đầu bán cùng Merly","Cách lấy và chia sẻ link","Cách tư vấn size","Cách đăng bài Facebook/Zalo","Cách xử lý khách hỏi giá/size","Chính sách hoa hồng và đơn hợp lệ","Cách gửi yêu cầu gắn đơn","Cách nhận thanh toán"];export default async function Page(){await requireAdminSession();const lessons=await db.partnerTrainingLesson.findMany({include:{_count:{select:{progress:true}}},orderBy:[{status:"asc"},{orderIndex:"asc"},{createdAt:"desc"}]});return <DashboardShell admin><div className="grid gap-6 lg:grid-cols-[0.9fr_1.2fr]"><form method="post" action="/admin/training/save" className="card space-y-3"><h1 className="text-3xl font-bold text-merly-900">Đào tạo</h1><input name="title" required className="input" placeholder="Tiêu đề bài học"/><textarea name="description" className="input min-h-20" placeholder="Mô tả ngắn"/><select name="category" className="input">{cats.map(c=><option key={c}>{c}</option>)}</select><div className="grid gap-3 md:grid-cols-3"><select name="level" className="input"><option value="beginner">beginner</option><option value="intermediate">intermediate</option><option value="advanced">advanced</option></select><input name="estimatedMinutes" type="number" className="input" placeholder="Phút"/><input name="orderIndex" type="number" className="input" placeholder="Thứ tự"/></div><input name="videoUrl" className="input" placeholder="Video URL (tuỳ chọn)"/><input name="thumbnailUrl" className="input" placeholder="Thumbnail URL (tuỳ chọn)"/><textarea name="body" className="input min-h-40" placeholder="Nội dung bài học"/><div className="grid gap-3 md:grid-cols-2"><select name="status" className="input"><option>draft</option><option>published</option><option>archived</option></select><input name="publishAt" type="datetime-local" className="input"/></div><label className="flex gap-2 text-sm"><input type="checkbox" name="announce"/> Tạo thông báo CTV khi xuất bản</label><button className="btn-primary">Lưu bài học</button></form><section className="card"><h2 className="text-2xl font-bold text-merly-900">Danh sách bài học</h2><div className="mt-4 space-y-3">{lessons.map(l=><article key={l.id} className="rounded-xl border border-rose-100 p-4"><div className="flex flex-wrap justify-between gap-3"><b>{l.title}</b><span className="text-sm">{l.status}</span></div><p className="text-sm text-stone-600">{l.category} · {l.level} · {l.estimatedMinutes??"—"} phút · Hoàn thành: {l._count.progress}</p><p className="mt-1 line-clamp-2 text-sm text-stone-600">{l.description}</p>{l.status!=="archived"?<form method="post" action="/admin/training/archive" className="mt-2"><input type="hidden" name="id" value={l.id}/><button className="font-semibold text-merly-700">Lưu trữ</button></form>:null}</article>)}</div></section></div></DashboardShell>}
