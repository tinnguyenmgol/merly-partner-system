import {DashboardShell} from "@/components/layout/dashboard-shell";import { requirePartnerSession } from "@/features/auth/partner-auth";export const dynamic="force-dynamic";export default async function Page(){const s=await requirePartnerSession();const p=s.account.partner;return <DashboardShell><div className="card"><h1 className="text-3xl font-bold text-merly-900">Tài khoản</h1><dl className="mt-4 grid gap-3 md:grid-cols-2">{[["Tên",p.displayName],["Số điện thoại",p.phone??"—"],["Email",p.email??"—"],["Ngân hàng",[p.profile?.bankName,p.profile?.bankAccountNumber].filter(Boolean).join(" · ")||"—"],["Trạng thái",s.account.status],["Lần đăng nhập cuối",s.account.lastLoginAt?.toLocaleString("vi-VN")??"—"]].map(([l,v])=><div className="rounded-xl bg-rose-50 p-3" key={l}><dt>{l}</dt><dd className="font-semibold">{v}</dd></div>)}</dl></div></DashboardShell>}