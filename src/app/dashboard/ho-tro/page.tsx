import {DashboardShell} from "@/components/layout/dashboard-shell";import { requirePartnerSession } from "@/features/auth/partner-auth";export const dynamic="force-dynamic";export default async function Page(){await requirePartnerSession();return <DashboardShell><div className="card"><h1 className="text-3xl font-bold text-merly-900">Hỗ trợ</h1><p className="mt-3 text-stone-600">Thông tin chỉ hiển thị cho tài khoản CTV đang đăng nhập.</p></div></DashboardShell>}