import { DashboardShell } from "@/components/layout/dashboard-shell";import { requirePartnerSession } from "@/features/auth/partner-auth";export const dynamic="force-dynamic";export default async function Page(){const s=await requirePartnerSession();const code=s.account.partner.codes[0]?.code??"<PartnerCode>";return <DashboardShell><div className="card space-y-5"><h1 className="text-3xl font-bold text-merly-900">Mã giới thiệu</h1><p className="text-stone-600">Link này dùng để ghi nhận doanh thu CTV, không bắt buộc giảm giá cho khách.</p><div className="rounded-xl bg-rose-50 p-4 font-mono text-sm text-merly-900">https://merlyshoes.com/?ref={code}</div><div className="rounded-xl bg-rose-50 p-4 font-mono text-sm text-merly-900">https://merlyshoes.com/products/&lt;handle&gt;?ref={code}</div><button className="btn-secondary" type="button">Copy link</button></div></DashboardShell>}